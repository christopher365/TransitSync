# Optional. If unset, the app derives the WebSocket URL from whatever host
# was used to load the page (see src/useVehicleWebSocket.js) — which is
# usually what you want, including when viewing from another device on the
# same network. Only set this to override that, e.g. a different backend port.
VITE_WS_URL=
