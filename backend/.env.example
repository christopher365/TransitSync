# Copy this file to .env and fill in real values for local (non-Docker) runs.
# docker-compose.yml sets these directly for the containerized backend instead.

DATABASE_URL=postgresql+psycopg://transitsync:transitsync@localhost:5432/transitsync
MBTA_API_BASE_URL=https://api-v3.mbta.com
MBTA_API_KEY=
